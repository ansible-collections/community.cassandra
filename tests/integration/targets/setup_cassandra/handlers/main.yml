- name: redhat_remove_cassandra
  yum:
    name: "{{ cassandra_yum_pkg }}"
    state: absent

- name: debian_remove_cassandra
  apt:
    name: "{{ cassandra_deb_pkg }}"
    state: absent

- name: Reset the Python Env for Debian
  import_tasks: reset_python_env.yml
